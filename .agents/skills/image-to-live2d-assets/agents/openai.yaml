interface:
  display_name: "Image to Live2D Assets"
  short_description: "Extract, recompose, and evaluate source-preserving Live2D assets"
  default_prompt: "Use $image-to-live2d-assets with this approved character spec and source image to create a canonical queue, extract source-preserving parts, recompose and evaluate them, and requeue only failed parts without claiming unconnected backends are complete."
