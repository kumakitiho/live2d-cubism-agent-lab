schema_version: 1
project: sample-one-image-model
source_stage: live2d-cubism-workflow
target_stage: image-to-live2d-assets
model_refs:
  model_import_psd: generated/model_import.psd
  layer_map: examples/layer_map.sample.yaml
  action_plan_report: outputs/action_plan_report.md
feedback:
  - id: feedback-face-hidden-fill-001
    target_layer_id: face_hidden_fill
    issue_type: deformation_break
    severity: high
    evidence:
      kind: screenshot
      path: outputs/auto_mesh_after.png
      description: 顔を右へ向けると前髪下の塗り足しが不足して肌に穴が見える。
    requested_action:
      action: inpaint
      details: face_hidden_fillの右側をsourceと同じ線・影で16px以上補完する。
    status: open
